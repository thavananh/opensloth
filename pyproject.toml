[project]
name = "HyperSloth"
version = "0.1.0"
description = ""
authors = [{ name = "anhvth", email = "anhvth.226@gmail.com" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pyyaml (>=6.0.2,<7.0.0)"]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "HyperSloth"
version = "0.1.0"
packages = [
    { include = "HyperSloth" }
]

[tool.poetry.scripts]
hypersloth = "HyperSloth.scripts.hypersloth:main"